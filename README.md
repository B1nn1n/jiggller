Pro-Tip for "True" Stealth:
If you are worried about someone seeing a command prompt window open on your taskbar, you can rename your script from jiggler.py to jiggler.pyw.

The .pyw extension tells Windows to run the script using pythonw.exe, which launches the GUI without opening a black terminal window in the background. It makes the app look like a standard, professional Windows utility.
